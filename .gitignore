# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# IDE
.idea/
*.iml
.vscode/
.settings/
.classpath
.project
bin/

# Runtime artifacts (generated by runClient)
runs/
run/

# Logs
logs/
*.log
crash-reports/
options.txt
screenshots/
saves/
.dat*
usernamecache.json
usercache.json
known_commits.txt

# Design working files (the shipped icon lives in src/main/resources/)
/icon.png
color-palette.png

# Local safety backups of shipped icons (made before batch edits)
backup/

# Third-party jars (resolved from the Modrinth maven at build time instead)
libs/

# tools/: local-only asset generators and research scratch - kept out of the
# repository (the .ps1 generators also claimed a 22% PowerShell slice on
# GitHub's language bar).
tools/

# docs/: local-only art research and collaboration materials (reference
# photos, palette generators, art guides embedding third-party textures) -
# only the published banner.png is tracked.
docs/*
!docs/banner.png

# TRAE agent tooling artifacts
.trae-html-share-packages/

# release-docs/: local-only platform release notes (Modrinth/CurseForge/
# MCMod descriptions and changelog drafts) - published content is pasted
# to the platforms directly, not shipped in the repo.
release-docs/

# OS
Thumbs.db
.DS_Store
